# HookerLib

Hooking helper library used by DisciplesGL.

This folder is included so the matching HookerLib source and bundled import artifacts are available with the DisciplesGL source tree.

Bundled outputs:

- `builds\hooker.lib`
- `builds\hooker.h`
